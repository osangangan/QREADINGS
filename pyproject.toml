[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qreadings-ai"
version = "0.1.0"
description = "Low-bloat cognitive architecture based on the QREADINGS Reconstruction Engine"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
